<Page x:Class="WpfApp1.Pages.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Главная">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="10">
            <TextBlock Text="Поиск:" VerticalAlignment="Center" Margin="5"/>
            <TextBox x:Name="SearchBox" Width="200" Margin="5"/>
            <Button Content="Найти" Click="Search_Click" Width="80" Margin="5"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="10">
            <TextBlock Text="Сортировка:" VerticalAlignment="Center" Margin="5"/>
            <ComboBox x:Name="SortComboBox" Width="150" Margin="5">
                <ComboBoxItem Content="По названию"/>
                <ComboBoxItem Content="По рейтингу"/>
            </ComboBox>
            <Button Content="Применить" Click="Sort_Click" Width="100" Margin="5"/>
        </StackPanel>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="MoviesList" Margin="10">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="Black" BorderThickness="1" Margin="5" Width="200" Height="300" Cursor="Hand" MouseLeftButtonDown="MovieBorder_MouseLeftButtonDown">
                            <StackPanel Margin="5">
                                <Image Source="{Binding image_path}" Width="180" Height="200" Stretch="UniformToFill"/>
                                <TextBlock Text="{Binding title}" FontWeight="Bold" Height="40" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding rating, StringFormat='Рейтинг: {0}'}"/>
                                <TextBlock Text="{Binding release_date, StringFormat='Дата: {0}'}"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>