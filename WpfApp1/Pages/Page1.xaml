<Page x:Class="WpfApp1.Pages.Page1"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Конструктор ПК">
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <GroupBox Header="Каталог комплектующих" Grid.Column="0" Margin="0,0,5,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBox x:Name="SearchBox" Width="200" Margin="0,0,10,0" 
                             TextChanged="SearchBox_TextChanged" VerticalContentAlignment="Center"/>
                    <ComboBox x:Name="TypeFilter" Width="150" SelectionChanged="Filter_Changed" 
                              VerticalContentAlignment="Center"/>
                    <ComboBox x:Name="ManFilter" Width="150" SelectionChanged="Filter_Changed" 
                              VerticalContentAlignment="Center"/>
                </StackPanel>

                <DataGrid x:Name="ProductsGrid" Grid.Row="2" AutoGenerateColumns="False" 
                          IsReadOnly="True" SelectionMode="Single" HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Тип" Binding="{Binding PartType.Name}" Width="100"/>
                        <DataGridTextColumn Header="Производитель" Binding="{Binding Manufacturer.Name}" Width="120"/>
                        <DataGridTextColumn Header="Цена" Binding="{Binding Price, StringFormat={}{0:C0}}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>

                <Button Content="Добавить в сборку" Grid.Row="2" VerticalAlignment="Bottom" 
                        HorizontalAlignment="Right" Width="150" Height="30" Margin="10" 
                        Click="AddToBuild_Click"/>
            </Grid>
        </GroupBox>

        <GroupBox Header="Ваша сборка" Grid.Column="1" Margin="5,0,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <ListBox x:Name="BuildList" Grid.Row="0" Margin="5" DisplayMemberPath="Name"/>

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
                    <TextBlock Text="Итого: " FontWeight="Bold" VerticalAlignment="Center"/>
                    <TextBlock x:Name="TotalPriceText" Text="0" FontWeight="Bold" FontSize="16" 
                               VerticalAlignment="Center"/>
                </StackPanel>

                <TextBlock x:Name="ErrorText" Grid.Row="2" TextWrapping="Wrap" Foreground="Red" 
                           Margin="5" FontWeight="Bold"/>

                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="5">
                    <Button Content="Сохранить сборку" Width="120" Margin="5" Click="SaveBuild_Click"/>
                    <Button Content="Очистить" Width="80" Margin="5" Click="ClearBuild_Click"/>
                </StackPanel>
            </Grid>
        </GroupBox>
    </Grid>
</Page>