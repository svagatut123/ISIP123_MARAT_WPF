<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Background="#FFFFFF" >
            <Button Content="Товары" Click="ShowProducts_Click" Margin="5" Padding="10,5" FontSize="14"/>
            <Button Content="Корзина" Click="ShowCart_Click" Margin="5" Padding="10,5" FontSize="14"/>
            <Button Content="Оформить заказ" Click="ShowCheckout_Click" Margin="5" Padding="10,5" FontSize="14" Background="#50C878" Foreground="White"/>
        </StackPanel>

        <Frame x:Name="MainFrame" Grid.Row="1" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>